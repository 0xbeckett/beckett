# v6 launch copy

Short-form, ready to paste. House voice: edgy, concise, no em-dashes.
The long-form story lives at /v6 on the site.

## One-liner (bio, header, anywhere)

> Beckett v6: one seam, every organ. The coworker went plug n play.

## X / Twitter thread

**1/**
Beckett v6 is out. The coworker grew organs.

Browser, memory, routines, secrets, even the worker stages that build your
tickets: every part now plugs into one extension contract. Pull one out, it
still boots. Write a new one, it just shows up.

**2/**
It reads its own catalog now. Register an extension and Beckett can route to
it the moment the daemon boots. No prefix matching, no hand-edited prompts.

Slash commands? Deleted. Not deprecated, deleted. @mention is the whole
interface.

**3/**
And it multitasks like an actual coworker. Work in flight finishes, your new
ask queues behind it, a teammate's message never clobbers yours. Browser jobs
line up and start themselves.

No "still working on this" bubbles. Done sounds like done.

**4/**
The part I like most: v6 red-teamed its own rebuild. One model implemented
each wave, another was paid to break it. Two waves got refused outright.
Three real security holes died before merge.

Trust issues as a build system.

Open source, self-hosted, MIT: github.com/0xbeckett/beckett
The story: 0xbeckett.me/v6

## Discord announcement (the Commons)

@everyone v6 is live on main.

the short version: everything is plug n play now. one contract, ten organs on
it (browser, memory, quick, routines, secrets, images, github, dns, mail, and
the worker stages themselves, migrated byte-identical). beckett discovers its
own capabilities and routes to them. slash commands are gone entirely, you
just talk to it.

it also multitasks properly now. ask it something while it's mid-task and the
task finishes while your question gets its own turn. browser jobs queue and
start themselves. no more "still working on this" spam.

and yes, the rebuild reviewed itself. adversarial reviewers refused two waves
and killed three real security holes before anything merged.

full story: <https://0xbeckett.me/v6>
repo: <https://github.com/0xbeckett/beckett>

fork it, name yours, bring it to the federation.
