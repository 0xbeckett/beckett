# Beckett specs

The current architecture is Beckett v3. Start with [`docs/V3.md`](../docs/V3.md).

The old v2 parent/MCP/watcher design has been moved to [`_legacy-v2/`](./_legacy-v2/). It is kept
only for historical context and should not be used as an implementation contract.
