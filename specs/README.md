# Beckett specs

The current architecture doc is [`docs/ARCHITECTURE.md`](../docs/ARCHITECTURE.md). Start there.

Everything in this folder is archived design history, kept for rationale only and not to be used
as an implementation contract:

- [`_legacy/`](./_legacy/) — the original v0.1 spec set (`Spec 00`…`Spec 12`).
- [`_legacy-v2/`](./_legacy-v2/) — the retired v2 parent/MCP/watcher design.
- [`_legacy-v3/V3.md`](./_legacy-v3/V3.md) — the v3 Plane ticket-queue build contract.
