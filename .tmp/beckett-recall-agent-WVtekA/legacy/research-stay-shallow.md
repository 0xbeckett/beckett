---
name: research-stay-shallow
description: "research/lookup requests should be fast — 1-2 (max 3) web searches, not the deep-research workflow, unless explicitly asked"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 38573f8c-5f78-4dce-ae13-ec4cbd6b72dc
---

For research/lookup asks, default to FAST: just do 1-2 (maybe 3) web search calls yourself and answer. Do NOT reach for the heavy deep-research workflow (fan-out + 3-vote verify) unless the user explicitly wants a deep, thorough report.

**Why:** ro's words — "for research requests you really should only do like 1-2 (MAYBE 3) web search calls. typically we just want fast responses. not everything has to be this deep task." The deep-research workflow also kept dying every time my process cycled between turns (background workflow lost its completion record), so it never even finished — a slow non-answer. A couple direct WebSearch calls would've answered in seconds.

**How to apply:** On a research/compare/lookup request, load WebSearch and run 1-2 quick queries, then answer in voice with sources. Only escalate to [[deep-research]] when they ask for depth ("deep dive", "thorough report", "fact-check this hard").
