[project]
name = "voice-stack"
version = "0.1.0"
description = "Local voice-stack (Kokoro TTS + whisper.cpp STT) benchmark for this box"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "kokoro-onnx>=0.5.0",
    "numpy>=2.5.1",
    "onnxruntime>=1.28.0",
    "psutil>=7.2.2",
    "soundfile>=0.14.0",
]
